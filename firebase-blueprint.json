{
  "entities": {
    "LookbookItem": {
      "title": "LookbookItem",
      "description": "Custom image mapping for lookbook items",
      "type": "object",
      "properties": {
        "id": {
          "type": "number",
          "description": "Item ID (e.g. 1, 2, 21)"
        },
        "customImage": {
          "type": "string",
          "description": "Direct image URL"
        },
        "pin": {
          "type": "string",
          "description": "Admin pin for authorization to write this document"
        }
      },
      "required": ["id", "customImage", "pin"]
    },
    "CustomerReview": {
      "title": "CustomerReview",
      "description": "Customer uploaded ratings, feedback comments, and photos of installed doors",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique Review ID"
        },
        "name": {
          "type": "string",
          "description": "Customer's human identifier name"
        },
        "rating": {
          "type": "number",
          "description": "Star rating value (integer value between 1 and 5)"
        },
        "feedback": {
          "type": "string",
          "description": "Descriptive feedback, comments, and experience details"
        },
        "photoUrl": {
          "type": "string",
          "description": "Base64 compressed image of the installed door"
        },
        "timestamp": {
          "type": "number",
          "description": "Numeric milliseconds timestamp representing submission time"
        }
      },
      "required": ["id", "name", "rating", "feedback", "photoUrl", "timestamp"]
    }
  },
  "firestore": {
    "lookbook-items/{itemId}": {
      "schema": {
        "$ref": "LookbookItem"
      },
      "description": "Collection of customized lookbook item images"
    },
    "customer-reviews/{reviewId}": {
      "schema": {
        "$ref": "CustomerReview"
      },
      "description": "Collection of peer door installations, ratings, and feedbacks"
    }
  }
}
